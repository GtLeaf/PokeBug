# Spare Assert

Processed asset backups and previews used by the firmware.

- `generated/src_assets/`: generated C++ asset tables currently copied into `src/assets/`.
- `previews/`: preview sheets for checking generated sprite size and alignment.
- `extracted/`: processed per-frame PNGs before RLE conversion.
- `legacy/`: older loose spare assets kept for reference.
